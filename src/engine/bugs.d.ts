import type { Bug, Frame } from '../types';
export const BUGS: Bug[];
export function brokenBubble(vals: number[]): Frame[];
export function brokenBinary(vals: number[]): Frame[];
export function brokenInsertion(vals: number[]): Frame[];
export function brokenBfs(): Frame[];
